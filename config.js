const config = {
    db: {
        host: "localhost",
        user: "root",
        password: "<dbPass>",
        database: "blogdb"
    },
    email: {
        username: "<mail>",
        password: "<Pass>",
        from: "<mail>",
    }
}

module.exports = config;